import { KeyboardShortcutDirective } from './keyboard-shortcut.directive';

describe('KeyboardShortcutDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyboardShortcutDirective();
    expect(directive).toBeTruthy();
  });
});
